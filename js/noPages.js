//var link = document.querySelectorAll('.menu__list > li > a');
var link = document.querySelectorAll('.menu__list > li > a > p');

//console.log(link[4]);
//--console.log('Text: ' + link[0].innerText);

/* link.onclick = function () {
    if (link.innerText == '...') {
        alert('В анус себе тыкни');
    }
} */

/*--for (let elem of link) {
    if (elem.matches('innerText="..."')) {
        console.log('Text: ' + link[0].innerText);
        //--alert("В анус себе тыкни");
    }
}--*/

/*---for (let elem of link) {
    if (elem.matches('link[3].innerText="..."')) {
        console.log('Text: ' + link[0].innerText);
        //--alert("В анус себе тыкни");
    }
}---*/

//----link.forEach(link => console.log('Множество: ', link.querySelector('a').innerText));

/*link.onclick = function(event) {
    if (event.target.innerText == '...') {
        console.log('7767678');
    }
            //--alert("В анус себе тыкни");
}*/


/*
console.log('Link: ' + link.href);
console.log('Attribute: ' + link.getAttribute('href'));
console.log('Text: ' + link.innerText);


for (let elem of document.body.children) {
    if (elem.matches('a[href$="zip"]')) {
      alert("Ссылка на архив: " + elem.href );
    }
*/